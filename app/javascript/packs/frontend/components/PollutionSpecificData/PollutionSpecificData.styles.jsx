import styled from "styled-components";

export const PollutionOverviewText = styled.div`
  font-size: 2.1rem;
  line-height: 2.4rem;
  color: ${({ color }) => color};
  font-weight: bold;
  text-align: center;
  margin-top: 1.2rem;
`;
