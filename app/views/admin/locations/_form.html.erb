<%= simple_form_for [:admin, @location] do |f| %>
  <%= f.input :name %>
  <%= f.input :street %>
  <%= f.input :installation_id, label: 'Installation Id' %>
  <%= f.input :longitude %>
  <%= f.input :latitude %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
